services:
  farmapp:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./farmApp:/app
    environment:
      - GO_ENV=production
    command: ["./main"]